package com.harsh.visualizer.enums;

/**
 * Broad family an algorithm belongs to. Drives which request shape it needs
 * (searching requires a target value) and how the frontend groups it.
 */
public enum AlgorithmCategory {
    SORTING,
    SEARCHING
}
